const BASE_URI = '/calculadhora';

export const HOME_URI = BASE_URI;

const STEP_BASE_URI = `${BASE_URI}/steps`;
export const STEP_INIT_URI = `${STEP_BASE_URI}/init`;
export const STEP_LUNCH_START_URI = `${STEP_BASE_URI}/lunch-start`;
